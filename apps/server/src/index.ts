import { workspaceStatus } from "@fast-app/shared";

function main(): void {
  console.log(
    "Phase 1 server placeholder. Runtime services start in later phases.",
    workspaceStatus
  );
}

main();
